# Basic

The minimal `runcd.yaml` shape: one app, one environment, no extras.

Every other example in this directory builds on this shape by adding one
feature at a time — start here if you're new to the config format.

See [`runcd.yaml`](runcd.yaml).
